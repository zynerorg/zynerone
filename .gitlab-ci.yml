stages:
  - matrix

Matrix:
  stage: matrix
  parallel:
    matrix:
      - images: 
          - "acme-zynerone"
          - "clamd-zynerone"
          - "dockerapi-zynerone"
          - "dovecot-zynerone"
          - "netfilter-zynerone"
          - "olefy-zynerone"
          - "php-fpm-zynerone"
          - "postfix-zynerone"
          - "rspamd-zynerone"
          - "sogo-zynerone"
          - "solr-zynerone"
          - "unbound-zynerone"
          - "watchdog-zynerone"
          - "api-zynerone"
  script:
  - echo "Hello from $images"
